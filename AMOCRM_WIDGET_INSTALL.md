# 📦 Установка виджета в amoCRM

## Проблема: Виджет не отображается в карточке сделки

Виджеты в amoCRM работают по-другому, чем обычные веб-страницы. Есть **два способа** встроить калькулятор в amoCRM:

---

## ✅ Способ 1: Через Custom Fields (Рекомендуется)

Это самый простой способ без разработки виджета.

### Шаг 1: Создать кастомное поле-кнопку

1. В amoCRM перейдите: **Настройки → Поля → Сделки**
2. Нажмите **"Добавить поле"**
3. Выберите тип: **"Кнопка"** или **"Ссылка"**
4. Настройте:
   - **Название**: `Открыть калькулятор`
   - **URL**: `https://your-domain.com/widget?lead_id={ID}&domain={SUBDOMAIN}.amocrm.ru`
   - **Открывать в**: `Модальном окне` или `Новой вкладке`
5. Сохраните

### Шаг 2: Добавить в воронку

1. Перейдите в **Настройки → Воронки и статусы**
2. Выберите нужную воронку
3. В настройках карточки добавьте созданное поле
4. Сохраните

Теперь в каждой сделке будет кнопка "Открыть калькулятор"! 

---

## 🔧 Способ 2: Разработка собственного виджета

Для отображения виджета справа в карточке нужно создать интеграцию-виджет.

### ⚠️ Важно: Требования к виджету

amoCRM требует:
1. **ZIP-архив** с файлами виджета
2. **HTTPS-хостинг** для файлов виджета
3. **Подпись виджета** от команды разработки amoCRM (для маркетплейса)

### Структура виджета

Файлы уже созданы в `public/amocrm-widget/`:

```
public/amocrm-widget/
├── manifest.json      # Манифест виджета
├── script.js          # Основной код виджета
├── style.css          # Стили виджета
└── i18n/
    └── ru.json        # Переводы
```

### Шаг 1: Собрать ZIP-архив

```bash
cd public/amocrm-widget
zip -r widget.zip manifest.json script.js style.css i18n/
```

### Шаг 2: Загрузить виджет

**Вариант А: Приватный виджет (для вашего аккаунта)**

1. В amoCRM: **Настройки → Интеграции → Ваша интеграция**
2. Перейдите в раздел **"Виджеты"**
3. Нажмите **"Установить приватный виджет"**
4. Загрузите `widget.zip`
5. Заполните данные:
   - **Название**: Калькулятор
   - **Код виджета**: `calculator`
   - **URL виджета**: `https://your-domain.com/amocrm-widget/script.js`

**Вариант Б: Через маркетплейс (публичный виджет)**

Требует модерации amoCRM:
1. Отправьте виджет на проверку через [форму разработчика](https://www.amocrm.ru/developers/content/crm_platform/widgets)
2. После одобрения виджет появится в маркетплейсе
3. Установите его как обычное приложение

---

## 🎯 Способ 3: Iframe в описании сделки (Быстрое решение)

Если нужно прямо сейчас:

### Вариант 3.1: Через кастомное HTML-поле

1. Создайте кастомное поле типа **"Текст"** или **"Текстовое поле"**
2. В настройках включите **"Разрешить HTML"**
3. В сделке вставьте:

```html
<iframe 
  src="https://your-domain.com/widget" 
  width="100%" 
  height="600" 
  frameborder="0"
  style="border: 1px solid #e0e0e0; border-radius: 4px;"
></iframe>
```

⚠️ **Минус**: Придется вставлять вручную в каждую сделку

### Вариант 3.2: Через webhook + автоматизация

1. Создайте цифровую воронку в amoCRM
2. Настройте триггер на создание сделки
3. Через API добавляйте iframe в примечание:

```javascript
{
  "note_type": "common",
  "params": {
    "text": "<iframe src='https://your-domain.com/widget?lead_id=" + leadId + "' width='100%' height='600'></iframe>"
  }
}
```

---

## 📱 Способ 4: Мобильное приложение (для iOS/Android)

Если нужен доступ с мобильных устройств:

1. Создайте **Progressive Web App (PWA)** версию виджета
2. Добавьте в amoCRM как **Внешнюю ссылку**
3. Пользователи смогут открывать через меню

---

## 🔍 Проверка: Почему виджет не отображается

### Проверьте в amoCRM:

1. **Настройки → Интеграции → Ваша интеграция**
   - Статус: **Активна** ✅
   - Права доступа: **Все необходимые права предоставлены** ✅

2. **Виджеты**
   - Виджет **установлен** и **активирован** ✅
   - URL виджета **правильный** ✅
   - Локация: **Карточка сделки** (lcard) ✅

3. **В карточке сделки**
   - Обновите страницу (Ctrl+F5)
   - Проверьте правую колонку (там должен быть виджет)
   - Откройте консоль браузера (F12) - проверьте ошибки

### Типичные ошибки:

**❌ "Widget is not defined"**
- Файл `script.js` не загружается
- Проверьте URL виджета в настройках

**❌ "CORS Error"**
- Виджет пытается обратиться к API напрямую
- Используйте бэкенд-прокси для запросов

**❌ Виджет загружается, но не отображается**
- Проверьте `locations` в `manifest.json`
- Должно быть: `["lcard-1", "lcard-2", "lcard-3"]`

**❌ Виджет есть, но пустой**
- Проверьте метод `bind_actions` в `script.js`
- Убедитесь, что iframe создается и добавляется в DOM

---

## ✅ Рекомендуемое решение для вашего случая

**Прямо сейчас используйте Способ 1** (Custom Fields):

1. Создайте поле-кнопку в настройках полей сделки
2. URL: `https://your-domain.com/widget`
3. Добавьте поле в карточку сделки
4. Готово! Теперь есть кнопка "Открыть калькулятор" в каждой сделке

**В будущем можно сделать Способ 2** (собственный виджет):

1. Соберите ZIP из файлов в `public/amocrm-widget/`
2. Загрузите как приватный виджет в вашу интеграцию
3. Активируйте в настройках аккаунта

---

## 🆘 Если ничего не помогло

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что интеграция активна
3. Проверьте, что у интеграции есть все права
4. Попробуйте в режиме инкогнито
5. Очистите кэш amoCRM (Настройки → Очистить кэш)

## 📞 Поддержка amoCRM

- Документация: https://www.amocrm.ru/developers/content/crm_platform/widgets
- Чат разработчиков: https://t.me/amocrm_api
- Техподдержка: support@amocrm.ru

---

## 📝 Быстрый чеклист

- [ ] Интеграция создана в amoCRM
- [ ] OAuth авторизация пройдена (токены получены)
- [ ] Виджет установлен (Способ 1 или 2)
- [ ] Поле добавлено в карточку сделки
- [ ] Проверено на тестовой сделке
- [ ] Работает! 🎉

Успешной интеграции! 🚀
